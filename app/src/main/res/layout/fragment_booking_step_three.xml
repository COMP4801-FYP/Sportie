<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:padding="8dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/sport_ctr_name"
        android:text="Pokfulam Field"
        android:textSize="24sp"
        android:textColor="@color/themeBlue"
        android:paddingBottom="10dp"
        android:textStyle="bold"
        android:fontFamily="@font/montserrat_bold"
        android:gravity="center_horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        />

    <TextView
        android:id="@+id/sport_court_no"
        android:text="Court #1"
        android:textSize="16sp"
        android:paddingBottom="20dp"
        android:textStyle="italic"
        android:fontFamily="@font/montserrat_bold"
        android:gravity="center_horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/themeBlue"
        />

    <devs.mulham.horizontalcalendar.HorizontalCalendarView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/calendarView"
        android:background="@color/colorButton"
        app:selectorColor="@color/themeYellow"
        app:textColorNormal="@color/white"
        app:textColorSelected="@color/themeYellow"
        />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_time_slot"
        android:layout_marginTop="4dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@android:color/transparent"
        android:dividerHeight="8dp"
        />

</LinearLayout>